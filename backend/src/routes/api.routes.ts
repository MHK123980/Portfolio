// This file is kept for backward compatibility but is no longer used.
// All routes are defined in public.routes.ts and admin.routes.ts
import { Router } from 'express';
const router = Router();
export default router;
